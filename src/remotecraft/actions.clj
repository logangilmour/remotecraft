(ns remotecraft.actions)

(defn left [fn]
  (fn "ML"))

(defn right [fn]
  (fn "MR"))

(defn up [fn]
  (fn "MU"))

(defn down [fn]
  (fn "MD"))

(defn forward [fn]
  (fn "MF"))

(defn back [fn]
  (fn "MB"))

